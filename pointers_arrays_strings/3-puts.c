#include "main.h"
#include <stdio.h>
/* task three */

/**
 * _puts - prints a string.
 * @str: the string to print
 * Return: void
 */

void _puts(char *str)
{
	for (; *str != '\0'; str++);
	{
		puts(const char *str);
	}
	_putchar('\n');
}
